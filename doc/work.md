# 大纲

这是一项非常冗长的任务，建议您充分利用完整的输出上下文，充分利用子agent来处理。
建议您充分利用完整的输出上下文来处理——整体输入和输出 tokens 控制在 200k tokens，充分利用上下文窗口长度将任务彻底完成，避免耗尽 tokens。充分利用上下文窗口长度将任务彻底完成，避免耗尽 tokens。

## 原则

* 任何情况下，逻辑嵌套必须少于三层。
* 聚焦可能的性能瓶颈，向量化，静态化。以及减少内存分配。
* 所有同质功能的接口只保留一个性能最高、最易用的，使代码更清晰，不易误用。
* 禁止向后兼容性。
* 禁止使用const_cast（除非为了调用外部求解器的局部使用），mutable（除非为了缓存或者锁），friend，dynamic_cast，shared_ptr等关键字或功能。
* 删除冗余的成员变量、接口等。
* 比起创造新的类和接口，改造现有的类是更好的。
* 完成一块工作任务后：
  * 确保编译通过。
  * 验证`doc/validation.md`全部案例。
  * 拒绝向后兼容性，强制改写所有调用处，让代码更简洁，对以后的扩展更通用。
  * 验证编译运行结果，移除所有向后兼容的或容易误用的接口，防止冗余。
  * 提交一次代码，然后继续下一块工作。

## 工作任务1

* 基于ExprTk依赖，封装一个表达式解析器。
* 以便可以（带缓存地）解析变量表，以及T，V，u（位移向量场）等，从而方便地设定耦合系数。
* 将cases中material.xml的LinzRes项全部移除，而改写electricconductivity为等价的含温度T的表达式。
* 验证结果正确性。 
* 从而可以移除value_parser，简化material_database以统一对所有材料属性的处理, 并以此简化physics_problem_builder等，并改写测试。
* 拒绝向后兼容性，强制改写所有调用处，让代码更简洁。
* 当有多种可能的实现路径时，选择最干净，代码量最少的一种。
* 我们事实上应该在类型层面区分材料属性是标量还是矩阵，例如热传导系数就天然应该是矩阵，热容天然就该是标量，等等。
* ThermalExpansion按照原样解析，应该是矩阵。
* 所以原则上我们应该不再需要parseFirstNumber了，应当规范化解析材料属性（统一入口）
* 用有缓存的字符串表达式体系应该可以处理所有系数，不必在material_database.hpp 中有其他分立的系数存储方式。
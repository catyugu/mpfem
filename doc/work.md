# 大纲

这是一项非常冗长的任务，建议您充分利用完整的输出上下文，充分利用子agent来处理。
建议您充分利用完整的输出上下文来处理——整体输入和输出 tokens 控制在 200k tokens，充分利用上下文窗口长度将任务彻底完成，避免耗尽 tokens。充分利用上下文窗口长度将任务彻底完成，避免耗尽 tokens。

## 原则

* 任何情况下，逻辑嵌套必须少于三层。
* 代码越精简越好，抹除不必要的抽象，历史接口等。
* 聚焦可能的性能瓶颈，向量化，静态化。以及减少内存分配。
* 所有同质功能的接口只保留一个性能最高、最易用的，使代码更清晰，不易误用。
* 禁止向后兼容性。
* 禁止使用const_cast（除非为了调用外部求解器的局部使用），mutable（除非为了缓存或者锁），friend，dynamic_cast，shared_ptr等关键字或功能。
* 删除冗余的成员变量、接口等。
* 比起创造新的类和接口，改造现有的类是更好的。
* 完成一块工作任务后：
  * 确保编译通过。
  * 确保回归测试通过。
  * 验证`doc/validation.md`全部案例。
  * 拒绝向后兼容性，强制改写所有调用处，让代码更简洁，对以后的扩展更通用。
  * 验证编译运行结果，移除所有向后兼容的或容易误用的接口，防止冗余。
  * 提交一次代码，然后继续下一块工作。

## 工作任务1

* 彻底分离io模块中那些可能随着更多物理场和配置项目的加入需要改动（我需要能理解，需要有可读性，易用性）的代码层次，和那些通常完全不再需要动（我不需要理解，只需要追求极致性能）的代码层次。
* 表达式解析中，应当更整洁地处理单位：对每个表达式，解析单位以获得倍数，解析原始表达式获得值，用计算得到的值乘以倍数。
* 宁可牺牲一次性预处理时的性能换取运行时的性能。
* 消除代码里分支逻辑。全部配置文件中的内容都应该基于字符串用表达式解析器标准化处理。
* 清理冗余、重复的方法、变量等。
* 请进行破坏式重构，不需要考虑向后兼容性。

## 工作任务2

* 进行架构重构，物理场应该持有总是持有几个积分器列表而不是直接持有系数。
* 积分器应该持有系数的共享引用（原始指针）。积分器不允许直接持有任何系数。

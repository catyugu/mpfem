# 大纲

## 要求

* 这是一项非常冗长的任务，建议您充分利用完整的输出上下文，充分利用子agent来处理。
建议您充分利用完整的输出上下文来处理——整体输入和输出 tokens 控制在 200k tokens，充分利用上下文窗口长度将任务彻底完成，避免耗尽 tokens。充分利用上下文窗口长度将任务彻底完成，避免耗尽 tokens。
* 把工作任务分成多个可以独立编译，测试和验证的子任务，完成一块子任务后：
  * 确保编译通过。
  * 确保回归测试通过。
  * 验证`doc/validation.md`全部案例。
  * 拒绝向后兼容性，强制改写所有调用处，让代码更简洁，对以后的扩展更通用。
  * 验证编译运行结果，移除所有向后兼容的或容易误用的接口，防止冗余。
  * 提交一次代码，然后继续完成下一个子任务。

## 任务

* 我现在意图用BASIX取代我们硬编码的单元生成逻辑，而且希望调整我们的自由度布局方案（直接使用BASIX的）。
* 但是BASIX采取的是张量积顺序，和我们的CCW参考单元顺序，自由度布局等存在不同，因此需要转换。这需要彻底的修改。
* 我认为应该把permutation的逻辑置于参考单元层，保持网格和geometric_mapping为CCW顺序，因为在网格处理层面还是CCW排序更加适合于工业标准。
* 请观察basix的张量积排序, 进行详细的重构计划，然后再执行。
* 直接用basix定义取代原先的FiniteElement及其派生类。改写其他相关代码如ReferenceElement，以及相关测试。